CREATE TABLE "SYSTEM"."TRAIN_TICKET" 
   (	"TICKET_NO" NUMBER(20,0) NOT NULL ENABLE, 
	"PASENGER_NAME" VARCHAR2(20 BYTE), 
	"GENDER" VARCHAR2(20 BYTE), 
	"AGE" NUMBER, 
	"TRAVEL_FROM" VARCHAR2(20 BYTE), 
	"TRAVEL_TO" VARCHAR2(20 BYTE), 
	"TOTAL_FARE" FLOAT(126), 
	"DISCOUNTED_FARE" FLOAT(126), 
	 CONSTRAINT "TABLE1_PK" PRIMARY KEY ("TICKET_NO")